export interface BoxEquipment {
  imei ?: string,
  libelleEquipement ?: string,
  montantPenalite ?: string | undefined | null,
  statut ?: string,
  demandeRetour ?: string,
  typeEquipement ?: string,
  serviceId ?: string | undefined
}
